# Cà Mau Weather App&#10;&#10;Real-time weather app for Cà Mau wards with frontend (HTML/JS/CSS) and Python backend.&#10;&#10;## Setup&#10;1. Install Python dependencies: `pip install flask requests` (or from requirements.txt if added).&#10;2. Run server: `python server.py`&#10;3. Open `index.html` in browser or access http://localhost:5000&#10;&#10;Uses wards_coords.json for locations.&#10;
